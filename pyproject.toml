[project]
name = "cognitodarkmatter"
version = "1.0.0"
description = "OpenEnv Project"
authors = [{name = "Shanmukheswar"}]
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic"
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"